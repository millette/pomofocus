{
  "name": "pomofocus-webhook",
  "version": "0.1.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "echo Nothing to build.",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "better-sqlite3": "^10.0.0",
    "dotenv-safest": "^1.1.1",
    "fastify": "^4.27.0"
  }
}
